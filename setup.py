from setuptools import setup, find_packages

setup(
    name='lipdub',  # CHANGED THIS TO MATCH THE FOLDER NAME
    version='0.1.0',
    packages=find_packages(),
    install_requires=[], # Your required packages are in requirements.txt
)